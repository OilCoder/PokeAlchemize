# Bitácora — 2026-04-08

## 14:48 — Replanteamiento de generación de imágenes + sincronización .claude/

### Cambios técnicos

- `todo/PLAN.md` — reescrito completamente con nuevo flujo en 2 etapas: sprite (01→02→03) + fondo (04→05→06→07→08→09)
- `.claude/CLAUDE.md` — actualizado con nuevo stack (FLUX.1-Kontext-dev), tablas de rules/skills y modo de validación `warn`
- `.claude/rules/` — sincronizado con `OilCoder/claude-project-base`: agregados `code-change.md`, `doc-enforcement.md`, `docs-style.md`; renombrado `plan-writing.md` → `plan-format.md`
- `.claude/skills/` — eliminados `code-change/`, `debug-script/`, `doc-enforcement/`, `docs-style/`; agregados `bitacora/`, `plan-writing/`, `doc-enforce/`, `document/`, `debug/`
- `.claude/rules/project-guidelines.md` — reescrito con formato de tablas y modos de validación

### Decisiones de diseño

- Abandonar generación de imagen completa (escena + Pokémon) — no funcionaba correctamente, solo recoloreaba
- Nuevo enfoque: img2img con FLUX.1-Kontext-dev para cambios estructurales reales (forma de cola, orejas, rasgos físicos) sobre el sprite original
- Pipeline en 2 etapas independientes: primero sprite reimaginado, luego fondo por inpainting/outpainting
- Agentes existentes (02_biome → 08_style_conciliador) se conservan y renumeran para la fase de fondo
- Estilo Ken Sugimori fijado via WiroAI/pokemon-flux-lora (no más styles.json para sprites)
- `code-change` pasa a ser solo regla (siempre activa), eliminado como skill — alineado con nueva versión de Claude Code donde rules y skills funcionan independientemente

### Pendiente

- [ ] Ejecutar Phase 2: migración y renombrado del pipeline
- [ ] Verificar compatibilidad de FLUX.1-Kontext-dev con diffusers en el container Docker
- [ ] Actualizar `data/types.json` con campo `morph_traits` por tipo

### Aprendizajes

- SDXL img2img con strength bajo solo cambia superficies (color, textura) — no morfología
- FLUX.1-Kontext-dev es un editor de imágenes por instrucción, ideal para cambios estructurales
- En la versión actual de Claude Code, las rules son siempre activas y las skills se cargan bajo demanda de forma independiente — no deben duplicarse entre ambos sistemas

---

## 19:30 — Pipeline SD 1.5 + conciliador de prompts + prueba caption style LoRA

### Cambios técnicos

- `pipeline/01_morph_agent.py` — system prompt reescrito: output de tags SD-style (6 max, 1-3 words), eliminadas frases narrativas
- `pipeline/02_negative_agent.py` — reducido a 15 items max; eliminada regla que negaba colores originales del Pokémon (interfería con transformación)
- `pipeline/04_pokemon_agent.py` — system prompt reescrito: tags visuales SD-style (6 max), enfocado en rasgos del tipo nuevo sin sesgo de tipo original
- `pipeline/05_prompt_conciliador.py` — nuevo agente: consolida outputs de morph_agent + pokemon_agent en un prompt limpio de máximo 10 tags, anclado en la estructura corporal del Pokémon
- `pipeline/batch_runner.py` — integrado conciliador: llamada secuencial post-paralelo; fallback a concatenación directa si falla; filtro de tipos originales (`if target_type in pokemon["types"]`)
- `config.py` — cambiado `SPRITE_LORA` a `pokemon_v3_chatgpt_offset.safetensors` para prueba caption style; `OLLAMA_TIMEOUT` subido a 300s; `DEV_POKEMON_IDS` expandido a 4 Pokémon
- `data/pokemons.json` — agregado campo `structure` a los 4 Pokémon de prueba (bulbasaur, charmander, squirtle, pikachu): descripción corporal sin sesgo de tipo
- `data/types.json` — limpiado (campo `morph_traits` ya existía)
- `docker-compose.yml` — agregado volumen `./data/loras:/app/data/loras`
- `requirements.txt` — eliminado `rembg`; fijadas versiones mínimas de diffusers, transformers, compel
- `debug/dbg_caption_style_prompts.py` — script de prueba con 10 prompts escritos a mano en estilo Bulbapedia/caption para comparar vs tag-style

### Decisiones de diseño

- Abandonado rembg para transparencia: destruía imágenes de colores claros (Pikachu). Fondo plano blanco como solución provisional
- Prompt conciliador agregado para evitar duplicados y contradicciones entre morph_agent y pokemon_agent, y para respetar el límite de 77 tokens de CLIP
- Campo `structure` en pokemons.json: descripción corporal neutral (sin tipo) para anclar identidad del Pokémon en el prompt sin introducir sesgos del tipo original
- LoRA caption style (`pokemon_v3_chatgpt_offset.safetensors`) descargado y probado — sin diferencia visual notable respecto al v3 offset regular. El techo está en SD 1.5 + LoRA, no en el formato del prompt

### Pendiente

- [ ] Decidir: ajustar CFG/sampler con SD 1.5 o migrar a FLUX + WiroAI/pokemon-flux-lora
- [ ] Agregar campo `structure` a los 146 Pokémon restantes
- [ ] Revertir `SPRITE_LORA` a `pokemon_v3_offset.safetensors` o mantener caption style según decisión

### Aprendizajes

- El LoRA `pokemon_v3_chatgpt_offset` está entrenado con descripciones Bulbapedia resumidas por ChatGPT — requiere frases descriptivas, no tags. Sin embargo la diferencia visual con el v3 offset es mínima
- El techo real de calidad es SD 1.5 + este LoRA. Para saltar ese techo hay que cambiar a FLUX
- Los prompts narrativos causan truncamiento en CLIP (77 tokens). Tags SD-style son la solución correcta para SD 1.5
- Docker con `--rm` descarta archivos escritos fuera de los volúmenes montados — `debug/` no está montado, hay que usar `outputs/` para persistir resultados de debug
